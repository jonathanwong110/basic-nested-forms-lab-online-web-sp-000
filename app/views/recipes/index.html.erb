<% @recipes.each do |recipe| %>
  <h4><%= recipe.title %></h4>
  <ul>
    <% recipe.ingredients.each do |address| %>
      <li> <%= ingredients.name %> <%= ingredients.quantity %>
    <% end %>
  </ul>
<% end %>
