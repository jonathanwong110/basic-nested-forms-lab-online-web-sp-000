<h2> <%= @recipe.title %></h2>

<h3>Ingredients</h2>

<ul>
<% @recipe.ingredients.each do |ingredient| %>
  <li><%= "#{ingredient.name} X #{ingredient.quantity} X #{ingredient.recipe_id}" %></li>
<% end %>
</ul>
